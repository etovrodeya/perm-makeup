import photo from '../media/images/aboutImage.png'

const initialState = {
    photo: photo,
    alt: "Анастасия Алексеева фото",
}

export function aboutMeReducer(state = initialState) {
    return state
}